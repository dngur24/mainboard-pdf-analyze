version: '3.8'

services:
  app:
    build: .
    container_name: motherboard-pcie-checker
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: always
