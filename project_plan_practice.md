# 프로젝트 기획서: AMD 메인보드 PCIe 레인 공유 안내 서비스

## 1. 프로젝트 개요

### 주제 : 외부 API를 이용한  AMD AM5 소켓 메인보드의 PCIe 레인 공유 파악

현대적인 AMD AM5 메인보드는 제한된 CPU/칩셋 레인을 효율적으로 사용하기 위해 슬롯 간 대역폭을 공유한다. 본 프로젝트는 사용자가 복잡한 매뉴얼을 읽지 않고도 SSD와 그래픽카드 장착 시 발생할 수 있는 성능 저하나 슬롯 비활성화 문제를 직관적으로 파악할 수 있는 웹 서비스를 구축하는 것을 목표로 한다.

  ---
### 배경 : 
사우스 브릿지를 담당하는 메인보드 칩셋과 연결된 SSD 슬롯을 사용하게 될 경우, 강제적으로 일부 CPU와 연결된 기기에서 대역폭을 나눠 사용하게 된다. 이는 메인보드 칩셋을 2개 탑재하는 고급형 메인보드를 사용하면 해결되는 문제이지만, 비싼 가격으로 인하여 선듯 구매하기 쉽지 않다. 그렇다면, 처음 구매할 때부터 PCIe 레인 공유 문제를 알고 있다면 조립할 때 이를 신경써서 메인보드에 장착할 것이다. 이미 mobomap이라는 외국 사이트가 존재하지만, 국내에는 정발되지 않은 보드들도 많다. 정발된 보드만을 보고 싶을 때 사용하기 위해 해당 주제를 선택했다. 국내에 정발되지 않은 보드를 구매하는 사람이라면 이미 컴퓨터 하드웨어에 대해 많은 관심을 가진 사람일 것으로 추측할 수 있기 때문에 국내 정발 메인보드만을 타겟으로 한다.




---

## 2. 기능 정의서 (Functional Specification)

### 2.1. 사용자 기능 (User Features)
*   **F-01: 메인보드 통합 검색**
    *   사용자가 모델명, 브랜드, 칩셋(B650, X670 등)을 키워드로 보드를 검색할 수 있음.
*   **F-02: 인터랙티브 PCIe 시뮬레이터**
    *   특정 M.2 슬롯을 클릭하여 '장착' 상태로 전환 시, 해당 동작이 다른 PCIe 슬롯에 미치는 영향(속도 저하, 비활성화 등)을 실시간 시각화.
*   **F-03: 기술 사양 상세 조회**
    *   각 슬롯의 PCIe 버전(5.0/4.0), 레인 수(x16/x4), 데이터 소스(CPU/Chipset) 정보를 명시.
*   **F-04: 공유 규칙 안내**
    *   대역폭 공유가 발생하는 기술적 이유를 사용자 친화적인 한국어 텍스트로 설명 제공.

### 2.2. 관리자 및 자동화 기능 (Admin & Automation)
*   **F-05: Gemini AI 기반 매뉴얼 분석**
    *   비정형 텍스트(제조사 매뉴얼 스펙 표 등)를 입력하면 AI가 분석하여 정형화된 JSON 데이터로 자동 변환.
*   **F-06: 데이터 관리 시스템**
    *   RESTful API를 통해 메인보드 데이터를 등록, 수정, 삭제할 수 있는 기반 구축.

---

## 3. API 명세서 초안 (API Specification Draft)

모든 API 응답은 `application/json` 형식을 따릅니다.

### 3.1. 메인보드 리소스 관리
| 기능 | 메서드 | 엔드포인트 | 요청 파라미터 | 설명 |
| :--- | :--- | :--- | :--- | :--- |
| 목록 조회 | `GET` | `/api/motherboards` | `q` (검색어), `chipset` | 조건에 맞는 보드 목록 반환 |
| 상세 조회 | `GET` | `/api/motherboards/:id` | `id` (보드 고유 ID) | 특정 보드의 슬롯 및 공유 규칙 정보 반환 |
| 데이터 등록 | `POST` | `/api/motherboards` | `JSON body` | 새로운 메인보드 사양 정보 추가 |
| 데이터 수정 | `PUT` | `/api/motherboards/:id` | `JSON body` | 기존 보드 정보 업데이트 |

### 3.2. 유틸리티 및 AI 서비스
| 기능 | 메서드 | 엔드포인트 | 요청 바디 | 설명 |
| :--- | :--- | :--- | :--- | :--- |
| 매뉴얼 분석 | `POST` | `/api/analyze/manual` | `{"text": "스펙 텍스트"}` | Gemini AI를 통한 JSON 데이터 추출 |

---

## 4. 데이터 스키마 (v1.0)
```json
{
  "id": "asus-rog-strix-b650e-f",
  "name": "ASUS ROG STRIX B650E-F GAMING WIFI",
  "brand": "ASUS",
  "chipset": "B650E",
  "slots": [
    { "id": "pci_1", "name": "PCIEX16_1", "type": "PCIe 5.0 x16", "source": "CPU" }
  ],
  "storage": [
    { "id": "m2_1", "name": "M.2_1", "type": "PCIe 5.0 x4", "source": "CPU" }
  ],
  "sharingRules": [
    {
      "trigger": "m2_3",
      "impact": "pci_2",
      "effect": "disabled",
      "description": "M.2_3 슬롯 사용 시 PCIEX16_2 슬롯이 비활성화됩니다."
    }
  ]
}
```

---

## 5. 기대 효과
1.  **조립 사고 방지:** 부품 구매 전 대역폭 간섭을 미리 확인하여 중복 지출 및 반품 예방.
2.  **데이터 관리 효율화:** Gemini AI를 활용하여 사람이 수동으로 입력하던 시간을 90% 이상 단축.
3.  **확장성 확보:** RESTful API 구조를 채택하여 향후 모바일 앱 및 하드웨어 커뮤니티와의 데이터 연동 가능.
